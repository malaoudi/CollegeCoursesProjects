//
//  MyDynamicArray.TestDrivers.cpp
//  Assignment 5
//
//  Created by z on 3/6/18.
//  Copyright © 2018 210. All rights reserved.
//
#include <iostream>
#include "MyDynamicArray.h"
#include "MyDynamicArray.cpp"

using namespace std;
/*
int main() {
    
    
     //string tests
     MyDynamicArray<string,1>
     for (int i = i; i < cap; i++)
     values[i] = V( );
     
     // object copy test
     cout << "\nObject copy test\n";
     MyDynamicArray<int,2> d = a; // making a copy
     assert(a.capacity( ) == d.capacity( ));
     for (int i = 0; i < a.capacity( ); i++)
     assert(a[i] == d[i]); // uses the setter version for both a and d
     
     
     // object assignment test
     cout << "\nObject assignment test\n";
     MyDynamicArray<int,1> e; e = a;
     assert(a.capacity( ) == e.capacity( ));
     for (int i = 0; i < a.capacity( ); i++)
     assert(a[i] == e[i]);
     
     
     const int SIZE = 10; // Number of elements
     int count; // Loop counter
     
     MyStaticArray<int,100> a2(SIZE);
     MyStaticArray<double, 100>a3(SIZE);
     
     for (count = 0; count < SIZE; count++)
     {
     a2[count] = (count + 2);
     a3[count] = (count + 1.5);
     }
     cout << "These values are in a2:\n";
     for (count = 0; count < SIZE; count++)
     cout << a2[count] << " ";
     cout << endl;
     cout << " values are in a3:\n";
     for (count = 0; count < SIZE; count++) {
     cout << a3[count] << " ";
     a2.getElementat(2);
     }
     
 
    return 0;

*/

